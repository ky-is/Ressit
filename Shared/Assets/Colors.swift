import UIKit
import SwiftUI

extension Color {
	static let background = Color(UIColor.systemBackground)
	static let tint = Color(.tint)
}

extension UIColor {
	static let tint = UIColor(red: 0.9, green: 0.3, blue: 0.5, alpha: 1)
}
